Django>=5.0,<6.0
django-environ
PyPDF2
python-docx
openpyxl
# Layout-aware PDF inspection for the ATS checker (columns, images, tables, fonts).
pdfplumber
# NLP for the ATS checker: PorterStemmer works out of the box. WordNet synonyms
# additionally need `python -m nltk.downloader wordnet`; without that corpus the
# checker falls back to its curated technical synonym map.
nltk
requests
google-api-python-client
google-auth
google-auth-httplib2
google-auth-oauthlib
gspread
openai>=1.0
apify-client
reportlab
django-crispy-forms
crispy-bootstrap5

# Async processing & deployment
celery>=5.3
redis>=5.0
gunicorn
whitenoise
psycopg2-binary

# Timezone database. python:3.10-slim ships without the system tzdata, so
# zoneinfo cannot resolve names like "Europe/London" in the container and every
# localised timestamp breaks (it works locally only because the dev machine has
# tzdata). Installing it as a wheel makes the container match local behaviour.
tzdata

# Optional: error tracking in production
sentry-sdk
