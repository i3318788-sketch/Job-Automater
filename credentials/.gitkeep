# Place your Google service-account credentials JSON in this folder.
# In docker-compose it is mounted read-only at /app/credentials.
# Point GOOGLE_SHEETS_CREDENTIALS_JSON at e.g. /app/credentials/service-account.json
